import { ConditionLabelPipePipe } from './condition-label-pipe-pipe';

describe('ConditionLabelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionLabelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
